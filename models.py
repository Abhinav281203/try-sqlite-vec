class Row:
    def __init__(self, values, embeddings=None):
        self.values = values
        self.embeddings = embeddings

    def __repr__(self):
        return str(self.values)

    def __str__(self):
        return str(self.values)
